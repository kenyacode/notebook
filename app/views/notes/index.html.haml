.wrapper_with_padding
	#notes.clearfix
		- unless @notes.blank?
			- @notes.each do |note|
				%a{ href: (url_for [note])}
					.note
						%p.title= note.title
						%p.date= time_ago_in_words(note.created_at)
		- else
			%h2 Add a note
			%p It appears you haven't created any scribbles yet... Lets fix that.  Why don't you create a new scribble below.
			%button= link_to "New Scribble", new_note_path
